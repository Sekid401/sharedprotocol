# sharedprotocol
SharedProtocol - Connect from Your browser To your Terminal

# How does it Connect?
it uses an browser to Load and Connect to SharedProtocol, The browser is Headless

# Do every SP features work in the terminal?
no

# What if im on android?
run sharedprotocol with 2>/dev/null
